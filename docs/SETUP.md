# Setup

1. شغّل PostgreSQL أو `docker compose up -d db`.
2. انسخ `backend/.env.example` إلى `backend/.env`.
3. شغّل FastAPI.
4. شغّل Next.js.
5. اختبر البحث.
6. أضف Telegram token لتشغيل البوت.

للمرحلة التالية: إعداد credentials لكل مصدر وظائف، ثم تنفيذ adapter مستقل واختباره.
